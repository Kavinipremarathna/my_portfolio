{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Full-stack portfolio application",
  "private": true,
  "scripts": {
    "dev": "kill-port 5173 5000 && concurrently \"cd client && npm run dev\" \"cd server && npm start\"",
    "build": "cd client && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "kill-port": "^2.0.1"
  }
}
